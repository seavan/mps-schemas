{
  "type": "object",
  "oneOf": [
    {"$ref": "#/definitions/success"},
    {"$ref": "#/definitions/fail"},
    {"$ref": "#/definitions/progress"}
  ],

  "definitions": {
    "progress": {
      "additionalProperties": false,
      "required": ["id", "type", "callback_uri", "params"],
      "properties": {
        "id": {"type": "integer"},
        "callback_uri": {"type": "string"},
        "type": {"enum": ["PROGRESS"]},
        "params": {
          "type": "object",
          "additionalProperties": false,
          "required": ["progress"],
          "properties": {
            "progress": {"type": "integer"}
          }
        }
      }
    },

    "success": {
      "additionalProperties": false,
      "required": ["id", "type", "callback_uri", "params"],
      "properties": {
        "id": {"type": "integer"},
        "callback_uri": {"type": "string"},
        "type": {"enum": ["SUCCESS"]},
        "params": {
          "type": "object",
          "oneOf": [
            {"$ref": "#/definitions/success_params/parse_metadata"},
            {"$ref": "#/definitions/success_params/upload"},
            {"$ref": "#/definitions/success_params/delete"}
          ]
        }
      }
    },
    "success_params": {
      "parse_metadata": {
        "additionalProperties": false,
        "required": ["album", "artist", "title"],
        "properties": {
          "album": {"type": "string"},
          "artist": {"type": "string"},
          "title": {"type": "string"}
        }
      },
      "transcode_av": {
        "additionalProperties": false,
        "properties": {}
      },
      "transcode_a": {
        "additionalProperties": false,
        "properties": {}
      },
      "upload": {
        "additionalProperties": false,
        "required": ["video_id"],
        "properties": {
          "video_id": {"type": "string"}
        }
      },
      "delete": {
        "additionalProperties": false,
        "properties": {}
      }
    },

    "fail": {
      "additionalProperties": false,
      "required": ["id", "type", "callback_uri", "params"],
      "properties": {
        "id": {"type": "integer"},
        "callback_uri": {"type": "string"},
        "type": {"enum": ["FAIL"]},
        "params": {
          "additionalProperties": false,
          "type": "object",
          "required": ["code", "message"],
          "properties": {
            "code": {"type": "integer"},
            "message": {"type": "string"}
          }
        }
      }
    }
  }
}
